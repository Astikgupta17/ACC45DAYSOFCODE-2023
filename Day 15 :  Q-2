#include <iostream>
using namespace std;

int main() {
	int t,n,i,max ;
	cin>>t;
	while(t--){
	    cin>>n;
	    int m[n];
	    for(i=0;i<n;i++)
	    cin>>m[i];
	    
	    max=0;
	    for(i=0;i<n;i++)
	    if(max<m[i])
	    max=m[i];
	    
	    cout<<max<<endl;
	}
	return 0;
}
